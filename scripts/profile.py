# TODO: if aggregate is true and fasta mode is true, aggregate position wise if alanines no
# cast to position wise and apply sliding window
# save as tsv position x score
import matplotlib.pyplot as plt
import pandas as pd
import re
from tqdm import tqdm
import os
import numpy as np
